import { FeCheck } from '../../assets/Icons';
import './Header.scss';

const Header = () => {
  return (
    <header className="App-header">
      <FeCheck />
      <h1 className="App-title">AfroToDo</h1>
    </header>
  );
};

export default Header;
